# Reviewer Package Notes

This directory was generated from the working repository by uild_reviewer_repository.ps1.

Before upload:

- Replace bracketed placeholders in README.md.
- Confirm whether the reviewer platform should receive eviewer_repository/ directly or eviewer_repository.zip.
- Upload large pretrained model assets separately if required. The default package references public/local backbone paths but does not include model checkpoints.
- Keep the package anonymous if the target review workflow requires anonymization.

